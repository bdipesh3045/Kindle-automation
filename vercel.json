{

  "builds": [
   {
      "src": "build_files.sh",
      "use": "@vercel/static-build",
      "config": {
      "distDir": "static"
       }
      }
  
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
